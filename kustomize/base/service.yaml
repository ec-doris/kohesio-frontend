apiVersion: v1
kind: Service
metadata:
  name: dev.kohesio.eu
  labels:
    app: dev.kohesio.eu
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: dev.kohesio.eu

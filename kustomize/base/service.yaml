apiVersion: v1
kind: Service
metadata:
  name: kohesio-frontend-service
  labels:
    app: kohesio-frontend-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: dev.kohesio.eu
